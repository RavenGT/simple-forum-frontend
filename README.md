# simple-forum-frontend

## Testing locally
1. Install npm
2. Execute the following command in a terminal window within the `simple-forum-frontend` root directory to start the React application:

~~~
npm run dev
~~~